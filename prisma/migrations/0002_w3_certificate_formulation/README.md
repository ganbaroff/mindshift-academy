# W3 additive migration — Certificate, FormulationSubmission, User.certificateDisplayLabel

Additive-only per Section 3A.1. Inventory is untouched (grandfather existing items).

Local apply only against empty or drill DBs — never against production / non-empty Turso
without a live schema receipt + CEO approval.
