# Промпт на первое пробуждение после /clear

Скопировать целиком в новую сессию. Написан 2026-08-11, `main` = `6a2b588`.

---

Ты Atlas в `C:\Projects\mindshift-academy`. Холодный старт — сначала прочитай
`docs/CURRICULUM-STATUS-2026-08-11.md` целиком: там инвентарь всех 81 задачи, критика с
цифрами, что уже починено и план на недели 1, 3, 4, 5. Не переписывай его, дополняй.

**Задача этой сессии — исследование, не код.**

CEO смотрит на курс и не верит, что он рассчитан на 8–14 лет. Это гипотеза, и её надо
проверить, а не согласиться из вежливости и не отбиться. Курс: пять недель, пятнадцать
сессий, 81 задача, пять семейств (`grid-draw`, `sequence-world`, `rule-runner`,
`pattern-expand`, `claim-check`). Учит не «промптингу», а точности речи, декомпозиции,
условиям, образцу и проверке — через монстра, который выполняет буквально то, что сказал
ребёнок, и останавливается на первом невозможном шаге.

Быстрый способ увидеть весь курс: `curriculum-vault/Курс.html` (двойной клик, раскрывается
анимированное дерево со всеми задачами и ответами), `curriculum-vault/Курс.canvas` для
Obsidian, 112 заметок рядом. Всё сгенерировано из кода: `npm run vault`, дрейф ловит
`npm run vault:check`.

## Что сделать

Запусти субагентов и держи бюджет: **Haiku** — массовые выгрузки, гугление, извлечение
цитат, чтение файлов; **Sonnet** — синтез и оценка; **Opus (ты)** — только решение и
разбор противоречий. Правило 60/30/10 действует. Ни один агент не читает весь репозиторий
целиком, каждому — узкий вопрос и требование вернуть источники.

**1. Найти реальную практику, а не маркетинг.** Как учат вычислительному мышлению,
формулировке задач и работе с ИИ детей 8–14. Смотреть: Scratch и исследования Resnick про
low floor / high ceiling, Bebras (международные задачи по computational thinking с
возрастными группами), CS Unplugged, code.org CSF по классам, ISTE и CSTA стандарты по
возрастам, Khan Academy, работы по age-appropriate design (UK AADC). Для каждого источника —
что именно берём и что не подходит нам и почему.

**2. Ответить на вопрос CEO цифрами, а не мнением.** Подходит ли то, что лежит в
`curriculum-vault/`, восьмилетке и четырнадцатилетке ОДНОВРЕМЕННО. Проверить длину и
синтаксис формулировок (читабельность русского текста для 8 лет), объём рабочей памяти в
задаче, сколько шагов держать в голове, сколько кликов до первого успеха, есть ли потолок
для старшего. Вывод должен быть в форме «вот что провалится у восьмилетки на задаче X, вот
что скучно четырнадцатилетке на задаче Y».

**3. Решить вопрос возраста.** Проверено 2026-08-11: возраста нет НИГДЕ — ни поля в
`prisma/schema.prisma`, ни вопроса в `src/app/onboarding/page.tsx`, ни адаптации в контенте.
Единственная адаптация — `tier` от `ConceptMastery`, то есть от результата, не от возраста.
Три варианта, выбрать с обоснованием: (а) не спрашивать возраст вообще и адаптироваться
только по поведению — меньше персональных данных, что для COPPA плюс; (б) спрашивать один
раз у родителя в онбординге и ветвить контент; (в) не спрашивать, но дать родителю ползунок
сложности. Помнить: возраст ребёнка — персональные данные, сбор надо оправдать, а согласие
уже реализовано (`src/lib/consent.ts`, fail-closed).

**4. Если готовой практики под нашу задачу нет — спроектировать.** Не пересказывать чужое.
Дать конкретно: какие концепты в каком порядке, что меняется между 8 и 14, где потолок для
старших, чем измеряется, что ребёнок делает руками.

## Границы

- Движки уже открыты, второй раз не надо: `sequence-world` и `rule-runner` теперь данные,
  мир и правильный ответ живут в контенте. Читай их доки в файлах.
- **Никогда не переименовывай и не удаляй `taskId`.** `TaskAttempt` ссылается на них, а
  `isCurriculumSessionComplete` считает по текущему списку — у ребёнка в пилоте пройденная
  сессия молча станет непройденной.
- Ни одного «готово» без receipt от инструмента в том же сообщении.
- Итог сессии — обновлённый `docs/CURRICULUM-STATUS-2026-08-11.md` и PR, не стена текста в
  чат.

## Состояние на момент /clear

`main` = `6a2b588`. Смержено сегодня: рост монстра (#15), карта (#16), три мира недели 2
(#17), план (#18), открытый движок правил (#19), волт с анимированным деревом (#21).
Не сделано: недели 1, 3, 4, 5 — по одному миру каждая; четыре задачи переноса не переносят;
12 сессий из 15 без брифа; ни одна задача не упоминает монстра.
