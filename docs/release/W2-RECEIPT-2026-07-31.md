# W2 Receipt — Learner state, return, mastery, recovery

**Date:** 2026-07-31  
**Status:** VERIFIED (W2 acceptance evidence green; no deploy/push)  
**Authority:** `docs/handoffs/CURSOR-MINDSHIFT-V1-IMPLEMENTATION.md` §W2 + §3A.1 + §3A.2

## Housekeeping (pre-W2)

1. Banned lexicon: `src/content/curriculum/week-1/session-3.ts` — removed child-facing «неправильно»; commit `5bb44c9`.
2. W1 scope note: `docs/release/W1-RECEIPT-2026-07-31.md` now records that `2d0783f` also touched `package.json`.

## Signed-in awareness (`/` + `/enter-code`)

- Pure policy: `signedInContinuePath` in `src/lib/academy-access.ts`
- API: `GET /api/continue` (`src/app/api/continue/route.ts`)
- UI: `src/components/access/SignedInContinue.tsx` on `/` (banner) and `/enter-code` (replaces dead code form when signed in)
- Behavior: signed-in → consent / onboarding / `/session/{next}` continue-path; never stuck on empty code form
- E2E receipt: `docs/release/_w2_drill_workspace/e2e-drill-receipt.md` (anonymous + gates; ALL GREEN)

## Resume derivation proof (refresh mid-session)

- Pure: `deriveResumeFromAttempts` in `src/lib/tasks/resume.ts`
- Persist: `TaskAttempt.sessionId` / `taskId` (additive); `persistTaskAttempt` writes them
- Read path: `listPassedTaskIds` prefers TaskAttempt, falls back to RewardEvent for legacy
- Unit drill: `tests/w2-learner-state.test.mjs` (mid-session index after 2 passes)

## Code re-entry drill (redeemed → same account)

- Contract unchanged + verified: `redeemAccessCode` returns same `clerkId` on `status===redeemed` with no state reset (`src/lib/access-code.ts`)
- Static drill assertions in `tests/w2-learner-state.test.mjs`

## Consent-revoked mid-session calm screen

- Route/UI: `src/app/session/[id]/page.tsx` — `data-testid="consent-ended-calm"`, copy «Сессия завершена», link home (no hard `/consent` bounce on mid-session revoke)
- Triggered on `CONSENT_REQUIRED` from session load / attempt / hint

## Onboarding residual copy alignment (diff summary)

- Removed: «Урок 1 из 5», «Пробуждение», «Начать урок», «3 слова» legacy Module-1 framing
- Added: «Сессия 1 из 15 · Неделя 1», «Монстр слышит только то, что сказано», «Начать сессию»
- Routing unchanged: `/session/w1-s1`

## Mastery-tier selection verification

- `selectOfferedTier` / `effectiveTaskTier` in `src/lib/tasks/tier-select.ts`
- Wired: session GET returns `mastery` + `offeredTier`; attempt route records `effectiveTaskTier(...)`
- Tests cover `tierForMastery` boundaries + wiring greps

## Additive migrations (Lane-4) — §3A.1 / §3A.2

| Artifact | Path |
|---|---|
| SQL delta | `prisma/migrations/0001_w2_additive_lane4/migration.sql` |
| README | `prisma/migrations/0001_w2_additive_lane4/README.md` |
| Models | `DegradeEvent`, `ReportDeliveryLog`, `SessionCost` + `TaskAttempt.sessionId/taskId` in `prisma/schema.prisma` |
| Helper | `src/lib/degrade-events.ts` (opaqueSessionId, dayBucket; no child text / Clerk ID / IP / precise timestamps) |
| Local proof | `docs/release/W2-ADDITIVE-MIGRATION-DRILL-2026-07-31.md` — **PASS** on non-empty file DB; rollback = restore `pre.db` |

**Not applied to any shared/non-empty Turso or production DB.**

## Consent-email ru-default regression

- `tests/w2-learner-state.test.mjs` asserts `locale ?? "ru"` in request-code route + consent UI posts `locale: "ru"`
- Also covered indirectly via deterministic consent source scans

## Lint / test totals

| Command | Result |
|---|---|
| `npm run lint` | exit 0 |
| `npm test` | **236 passed** = deterministic **72** + tasks **27** + interpreter-fixtures **65** + session/mastery **34** + w2-learner-state **38** |
| Additive drill | PASS |
| E2E W2 drills | ALL GREEN (`scripts/e2e/w2-drills.mjs`) |

Scope note: this wave also updates `package.json` (`test` / `test:w2` scripts).

## Unresolved red gates (carry-forward)

| Gate | Note |
|---|---|
| 9 unresolved P0s | readiness remnants → W3/W4 |
| 10 prod deploy without CEO | Not attempted |
| 11 push without CEO | Not attempted |
| Prod Turso schema | **UNKNOWN** — additive SQL prepared only; needs live receipt before any apply |
| Vendor string “Llama Guard” | Still absent as literal brand on consent |

Not tripped: consent breach, child-text retention in DegradeEvent, deploy, push, secrets, child invites.

## Commit hashes

- Housekeeping lexicon: `5bb44c9`
- W2 implementation: `ac791e3` (`ac791e3452edd479d7fb9d720eb19fe50103a5ec`) — includes `package.json` test-script expansion
- W2 receipt hash record: _(this commit)_

## Next safe wave

**W3** — Engagement, capstone, certificate, parent closure (milestone chest replacing gacha; `/certificate`; capstone flow).  
No W2 acceptance blocker. Do **not** start W4–W5 in the same pass.
