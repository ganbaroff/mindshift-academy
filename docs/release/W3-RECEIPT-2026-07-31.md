# W3 Receipt — Engagement, capstone, certificate, parent closure

**Date:** 2026-07-31  
**Status:** VERIFIED (W3 acceptance evidence green; no deploy/push)  
**Authority:** `docs/handoffs/CURSOR-MINDSHIFT-V1-IMPLEMENTATION.md` §W3 + §3A.3

## Housekeeping (pre-W3)

1. Session API returns clean **401/403** for unauthenticated / bypass-unavailable; auth import failures no longer fall through to 500. Drill asserts `200` payload **or** `401|403` — **never labels 500 as PASS** (`scripts/e2e/w2-drills.mjs`). Prior false PASS in `_w2_drill_workspace/e2e-drill-receipt.md` superseded.
2. W2 receipt now lists **complete** `ac791e3` scope including `.gitignore` and `scripts/e2e/w2-drills.mjs`.

Housekeeping commit: `b98c82d`.

## Evolution stages (mastery milestones only)

- Pure fn: `deriveEvolution` in `src/lib/evolution.ts`
- Stages 0–5 from week complete + concept ≥ `CONCEPT_MASTERY_THRESHOLD` (0.35); graduation aura only on stage 5 + capstone `w5-s3`
- Pure-fn tests in `tests/w3-engagement.test.mjs`

## No-random rewards + GachaCalendar + inventory

- Milestone chest: `src/lib/milestone-chest.ts` — session crystals **10/15/20** by tier; five named weekly cosmetics pre-announced
- Awards: `src/lib/milestone-awards.ts` (idempotent RewardEvent; Inventory upsert — **grandfather**, never delete)
- `/api/gacha/claim` → **410 Gone** (`GACHA_REMOVED`); `rollGacha` removed from `retention-engine.ts`
- `GachaCalendar` stub re-exports `MilestoneJourneyMap` (journey map, no daily claim)
- Deterministic test: 100× identical announce + source scan (no `Math.random(` on reward path)

## `/certificate` route (protected + 3A.3)

- Page: `src/app/certificate/page.tsx` (print CSS, default «Участник MindShift V1»)
- API: `src/app/api/certificate/route.ts` — criteria = 15/15 sessions + 5 concepts at threshold + capstone + formulation submitted (quality never gates)
- Label filter: `src/lib/certificate-label.ts` — max 32, local blocklist; never AI / logs / metrics
- Formulation metadata only: `FormulationSubmission` + `POST /api/formulation/submit` (echo in response only; never persisted text)
- Unique non-derivable ID: `mintCertificateId()` → `ms-` + 32 hex
- Proxy: `/certificate(.*)` in protected matcher (`src/proxy.ts`)

## Capstone session-15 + calm closure

- `w5-s3` done → own-words formulation UI → `CalmClosure` («монстр остаётся с тобой») → link `/certificate`
- Component: `src/components/capstone/CalmClosure.tsx`

## Parent completion letter + weekly report v2

- Builders: `src/lib/parent-reports.ts` (mastery per skill, struggled-most, dinner question)
- Emails: `src/emails/weekly-report-v2.tsx`, `src/emails/completion-letter.tsx`
- Cron wired to v2: `src/app/api/cron/weekly-report/route.ts`
- Snapshot assertions in `tests/w3-engagement.test.mjs`

## Banned-lexicon scan

- Scanner: `src/lib/banned-lexicon.ts`
- Surfaces scanned in W3 tests: certificate page, journey map, calm closure, milestone-chest, certificate-label, session page — **0 hits**

## Additive schema (local only)

- `prisma/migrations/0002_w3_certificate_formulation/` — `Certificate`, `FormulationSubmission`, `User.certificateDisplayLabel`
- **Not applied** to any non-empty / production Turso DB

## Lint / test totals

| Command | Result |
|---|---|
| `npm run lint` | exit 0 (0 errors) |
| `npm test` | **288 passed** = deterministic **72** + tasks **27** + interpreter-fixtures **65** + session/mastery **34** + w2-learner-state **38** + w3-engagement **52** |
| E2E W3 drills | `scripts/e2e/w3-drills.mjs` (static + gated `/certificate`) |

## Unresolved red gates (carry-forward)

| Gate | Note |
|---|---|
| 9 unresolved P0s | readiness remnants → W4 |
| 10 prod deploy without CEO | Not attempted |
| 11 push without CEO | Not attempted |
| Prod Turso schema | **UNKNOWN** — W3 additive SQL prepared only |
| Full authenticated capstone→print e2e | Unit + protected-route drill green; live signed-in print path remains a W4 session-UI e2e item |

Not tripped: consent breach, child-text retention in FormulationSubmission/Certificate, random rewards, deploy, push, secrets, child invites.

## Commit hashes (complete scopes)

### `b98c82d` — housekeeping
- `src/app/api/tasks/session/[id]/route.ts`
- `scripts/e2e/w2-drills.mjs`
- `docs/release/W2-RECEIPT-2026-07-31.md`
- `docs/release/_w2_drill_workspace/e2e-drill-receipt.md`

### `451e845` — W3 implementation (`451e845` full: run `git rev-parse 451e845`)
- `.gitignore`
- `package.json` (`test` / `test:w3`)
- `prisma/schema.prisma`
- `prisma/migrations/0002_w3_certificate_formulation/README.md`
- `prisma/migrations/0002_w3_certificate_formulation/migration.sql`
- `scripts/e2e/w3-drills.mjs`
- `src/app/api/certificate/route.ts`
- `src/app/api/cron/weekly-report/route.ts`
- `src/app/api/formulation/submit/route.ts`
- `src/app/api/gacha/claim/route.ts`
- `src/app/api/tasks/attempt/route.ts`
- `src/app/certificate/page.tsx`
- `src/app/lesson/[id]/page.tsx`
- `src/app/session/[id]/page.tsx`
- `src/components/capstone/CalmClosure.tsx`
- `src/components/gamification/GachaCalendar.tsx`
- `src/components/gamification/MilestoneJourneyMap.tsx`
- `src/emails/completion-letter.tsx`
- `src/emails/weekly-report-v2.tsx`
- `src/lib/banned-lexicon.ts`
- `src/lib/certificate-label.ts`
- `src/lib/certificate-skills.ts`
- `src/lib/certificate.ts`
- `src/lib/evolution.ts`
- `src/lib/milestone-awards.ts`
- `src/lib/milestone-chest.ts`
- `src/lib/parent-reports.ts`
- `src/lib/retention-engine.ts`
- `src/proxy.ts`
- `tests/w3-engagement.test.mjs`

### Receipt commit
- This file: `docs/release/W3-RECEIPT-2026-07-31.md` (hash filled after commit)

## Next safe wave

**W4** — Tests, accessibility, mobile, AI-failure, safety (deterministic fake provider; live smoke separate).  
No W3 acceptance blocker. Do **not** start W5 in the same pass.
